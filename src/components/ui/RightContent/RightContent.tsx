import ChatSection from '../../core/ChatSection/ChatSection'

const RightContent = () => {
  return (
    <div className="relative">
      <ChatSection />
    </div>
  )
}

export default RightContent
